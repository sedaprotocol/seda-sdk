// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: sedachain/core/v1/genesis.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { DataRequest, Params, RevealBody, Staker } from "./core";

/** GenesisState defines core module's genesis state. */
export interface GenesisState {
  params: Params | undefined;
  owner: string;
  pendingOwner: string;
  paused: boolean;
  /** Staking-related states: */
  allowlist: string[];
  stakers: Staker[];
  /** Data request-related states: */
  dataRequests: DataRequest[];
  committingCount: bigint;
  revealingCount: bigint;
  tallyingCount: bigint;
  commits: GenesisStateCommit[];
  reveals: GenesisStateReveal[];
}

/** GenesisStateCommit is used to store commits in the genesis state. */
export interface GenesisStateCommit {
  drID: string;
  publicKey: string;
  commit: Uint8Array;
}

/**
 * GenesisStateReveal is used to store revealers and their reveal bodies in the
 * genesis state.
 */
export interface GenesisStateReveal {
  drID: string;
  publicKey: string;
  revealBody: RevealBody | undefined;
}

function createBaseGenesisState(): GenesisState {
  return {
    params: undefined,
    owner: "",
    pendingOwner: "",
    paused: false,
    allowlist: [],
    stakers: [],
    dataRequests: [],
    committingCount: 0n,
    revealingCount: 0n,
    tallyingCount: 0n,
    commits: [],
    reveals: [],
  };
}

export const GenesisState = {
  encode(message: GenesisState, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).ldelim();
    }
    if (message.owner !== "") {
      writer.uint32(18).string(message.owner);
    }
    if (message.pendingOwner !== "") {
      writer.uint32(26).string(message.pendingOwner);
    }
    if (message.paused !== false) {
      writer.uint32(32).bool(message.paused);
    }
    for (const v of message.allowlist) {
      writer.uint32(42).string(v!);
    }
    for (const v of message.stakers) {
      Staker.encode(v!, writer.uint32(50).fork()).ldelim();
    }
    for (const v of message.dataRequests) {
      DataRequest.encode(v!, writer.uint32(58).fork()).ldelim();
    }
    if (message.committingCount !== 0n) {
      if (BigInt.asUintN(64, message.committingCount) !== message.committingCount) {
        throw new globalThis.Error("value provided for field message.committingCount of type uint64 too large");
      }
      writer.uint32(64).uint64(message.committingCount.toString());
    }
    if (message.revealingCount !== 0n) {
      if (BigInt.asUintN(64, message.revealingCount) !== message.revealingCount) {
        throw new globalThis.Error("value provided for field message.revealingCount of type uint64 too large");
      }
      writer.uint32(72).uint64(message.revealingCount.toString());
    }
    if (message.tallyingCount !== 0n) {
      if (BigInt.asUintN(64, message.tallyingCount) !== message.tallyingCount) {
        throw new globalThis.Error("value provided for field message.tallyingCount of type uint64 too large");
      }
      writer.uint32(80).uint64(message.tallyingCount.toString());
    }
    for (const v of message.commits) {
      GenesisStateCommit.encode(v!, writer.uint32(90).fork()).ldelim();
    }
    for (const v of message.reveals) {
      GenesisStateReveal.encode(v!, writer.uint32(98).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.owner = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.pendingOwner = reader.string();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.paused = reader.bool();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.allowlist.push(reader.string());
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.stakers.push(Staker.decode(reader, reader.uint32()));
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.dataRequests.push(DataRequest.decode(reader, reader.uint32()));
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.committingCount = longToBigint(reader.uint64() as Long);
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.revealingCount = longToBigint(reader.uint64() as Long);
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.tallyingCount = longToBigint(reader.uint64() as Long);
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.commits.push(GenesisStateCommit.decode(reader, reader.uint32()));
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.reveals.push(GenesisStateReveal.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisState {
    return {
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
      owner: isSet(object.owner) ? globalThis.String(object.owner) : "",
      pendingOwner: isSet(object.pendingOwner) ? globalThis.String(object.pendingOwner) : "",
      paused: isSet(object.paused) ? globalThis.Boolean(object.paused) : false,
      allowlist: globalThis.Array.isArray(object?.allowlist)
        ? object.allowlist.map((e: any) => globalThis.String(e))
        : [],
      stakers: globalThis.Array.isArray(object?.stakers) ? object.stakers.map((e: any) => Staker.fromJSON(e)) : [],
      dataRequests: globalThis.Array.isArray(object?.dataRequests)
        ? object.dataRequests.map((e: any) => DataRequest.fromJSON(e))
        : [],
      committingCount: isSet(object.committingCount) ? BigInt(object.committingCount) : 0n,
      revealingCount: isSet(object.revealingCount) ? BigInt(object.revealingCount) : 0n,
      tallyingCount: isSet(object.tallyingCount) ? BigInt(object.tallyingCount) : 0n,
      commits: globalThis.Array.isArray(object?.commits)
        ? object.commits.map((e: any) => GenesisStateCommit.fromJSON(e))
        : [],
      reveals: globalThis.Array.isArray(object?.reveals)
        ? object.reveals.map((e: any) => GenesisStateReveal.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    if (message.pendingOwner !== "") {
      obj.pendingOwner = message.pendingOwner;
    }
    if (message.paused !== false) {
      obj.paused = message.paused;
    }
    if (message.allowlist?.length) {
      obj.allowlist = message.allowlist;
    }
    if (message.stakers?.length) {
      obj.stakers = message.stakers.map((e) => Staker.toJSON(e));
    }
    if (message.dataRequests?.length) {
      obj.dataRequests = message.dataRequests.map((e) => DataRequest.toJSON(e));
    }
    if (message.committingCount !== 0n) {
      obj.committingCount = message.committingCount.toString();
    }
    if (message.revealingCount !== 0n) {
      obj.revealingCount = message.revealingCount.toString();
    }
    if (message.tallyingCount !== 0n) {
      obj.tallyingCount = message.tallyingCount.toString();
    }
    if (message.commits?.length) {
      obj.commits = message.commits.map((e) => GenesisStateCommit.toJSON(e));
    }
    if (message.reveals?.length) {
      obj.reveals = message.reveals.map((e) => GenesisStateReveal.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GenesisState>): GenesisState {
    return GenesisState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GenesisState>): GenesisState {
    const message = createBaseGenesisState();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    message.owner = object.owner ?? "";
    message.pendingOwner = object.pendingOwner ?? "";
    message.paused = object.paused ?? false;
    message.allowlist = object.allowlist?.map((e) => e) || [];
    message.stakers = object.stakers?.map((e) => Staker.fromPartial(e)) || [];
    message.dataRequests = object.dataRequests?.map((e) => DataRequest.fromPartial(e)) || [];
    message.committingCount = object.committingCount ?? 0n;
    message.revealingCount = object.revealingCount ?? 0n;
    message.tallyingCount = object.tallyingCount ?? 0n;
    message.commits = object.commits?.map((e) => GenesisStateCommit.fromPartial(e)) || [];
    message.reveals = object.reveals?.map((e) => GenesisStateReveal.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGenesisStateCommit(): GenesisStateCommit {
  return { drID: "", publicKey: "", commit: new Uint8Array(0) };
}

export const GenesisStateCommit = {
  encode(message: GenesisStateCommit, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.drID !== "") {
      writer.uint32(10).string(message.drID);
    }
    if (message.publicKey !== "") {
      writer.uint32(18).string(message.publicKey);
    }
    if (message.commit.length !== 0) {
      writer.uint32(26).bytes(message.commit);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GenesisStateCommit {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisStateCommit();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.drID = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.publicKey = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.commit = reader.bytes();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisStateCommit {
    return {
      drID: isSet(object.drID) ? globalThis.String(object.drID) : "",
      publicKey: isSet(object.publicKey) ? globalThis.String(object.publicKey) : "",
      commit: isSet(object.commit) ? bytesFromBase64(object.commit) : new Uint8Array(0),
    };
  },

  toJSON(message: GenesisStateCommit): unknown {
    const obj: any = {};
    if (message.drID !== "") {
      obj.drID = message.drID;
    }
    if (message.publicKey !== "") {
      obj.publicKey = message.publicKey;
    }
    if (message.commit.length !== 0) {
      obj.commit = base64FromBytes(message.commit);
    }
    return obj;
  },

  create(base?: DeepPartial<GenesisStateCommit>): GenesisStateCommit {
    return GenesisStateCommit.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GenesisStateCommit>): GenesisStateCommit {
    const message = createBaseGenesisStateCommit();
    message.drID = object.drID ?? "";
    message.publicKey = object.publicKey ?? "";
    message.commit = object.commit ?? new Uint8Array(0);
    return message;
  },
};

function createBaseGenesisStateReveal(): GenesisStateReveal {
  return { drID: "", publicKey: "", revealBody: undefined };
}

export const GenesisStateReveal = {
  encode(message: GenesisStateReveal, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.drID !== "") {
      writer.uint32(10).string(message.drID);
    }
    if (message.publicKey !== "") {
      writer.uint32(18).string(message.publicKey);
    }
    if (message.revealBody !== undefined) {
      RevealBody.encode(message.revealBody, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GenesisStateReveal {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisStateReveal();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.drID = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.publicKey = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.revealBody = RevealBody.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisStateReveal {
    return {
      drID: isSet(object.drID) ? globalThis.String(object.drID) : "",
      publicKey: isSet(object.publicKey) ? globalThis.String(object.publicKey) : "",
      revealBody: isSet(object.revealBody) ? RevealBody.fromJSON(object.revealBody) : undefined,
    };
  },

  toJSON(message: GenesisStateReveal): unknown {
    const obj: any = {};
    if (message.drID !== "") {
      obj.drID = message.drID;
    }
    if (message.publicKey !== "") {
      obj.publicKey = message.publicKey;
    }
    if (message.revealBody !== undefined) {
      obj.revealBody = RevealBody.toJSON(message.revealBody);
    }
    return obj;
  },

  create(base?: DeepPartial<GenesisStateReveal>): GenesisStateReveal {
    return GenesisStateReveal.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GenesisStateReveal>): GenesisStateReveal {
    const message = createBaseGenesisStateReveal();
    message.drID = object.drID ?? "";
    message.publicKey = object.publicKey ?? "";
    message.revealBody = (object.revealBody !== undefined && object.revealBody !== null)
      ? RevealBody.fromPartial(object.revealBody)
      : undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToBigint(long: Long) {
  return BigInt(long.toString());
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
