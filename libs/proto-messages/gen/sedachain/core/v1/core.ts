// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: sedachain/core/v1/core.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";

/** DataRequestStatus is the status of a data request. */
export enum DataRequestStatus {
  /** DATA_REQUEST_STATUS_UNSPECIFIED - Zero value as required by proto3. */
  DATA_REQUEST_STATUS_UNSPECIFIED = 0,
  /** DATA_REQUEST_STATUS_COMMITTING - Data request has been posted and is collecting commits. */
  DATA_REQUEST_STATUS_COMMITTING = 1,
  /**
   * DATA_REQUEST_STATUS_REVEALING - The number of commits has reached the replication factor, and the data
   * request is now collecting reveals.
   */
  DATA_REQUEST_STATUS_REVEALING = 2,
  /**
   * DATA_REQUEST_STATUS_TALLYING - The number of reveals has reached the replication factor, and the data
   * request is now ready to be tallied.
   */
  DATA_REQUEST_STATUS_TALLYING = 3,
  UNRECOGNIZED = -1,
}

export function dataRequestStatusFromJSON(object: any): DataRequestStatus {
  switch (object) {
    case 0:
    case "DATA_REQUEST_STATUS_UNSPECIFIED":
      return DataRequestStatus.DATA_REQUEST_STATUS_UNSPECIFIED;
    case 1:
    case "DATA_REQUEST_STATUS_COMMITTING":
      return DataRequestStatus.DATA_REQUEST_STATUS_COMMITTING;
    case 2:
    case "DATA_REQUEST_STATUS_REVEALING":
      return DataRequestStatus.DATA_REQUEST_STATUS_REVEALING;
    case 3:
    case "DATA_REQUEST_STATUS_TALLYING":
      return DataRequestStatus.DATA_REQUEST_STATUS_TALLYING;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DataRequestStatus.UNRECOGNIZED;
  }
}

export function dataRequestStatusToJSON(object: DataRequestStatus): string {
  switch (object) {
    case DataRequestStatus.DATA_REQUEST_STATUS_UNSPECIFIED:
      return "DATA_REQUEST_STATUS_UNSPECIFIED";
    case DataRequestStatus.DATA_REQUEST_STATUS_COMMITTING:
      return "DATA_REQUEST_STATUS_COMMITTING";
    case DataRequestStatus.DATA_REQUEST_STATUS_REVEALING:
      return "DATA_REQUEST_STATUS_REVEALING";
    case DataRequestStatus.DATA_REQUEST_STATUS_TALLYING:
      return "DATA_REQUEST_STATUS_TALLYING";
    case DataRequestStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Staker is an identity that has staked SEDA tokens to perform data request
 * execution.
 */
export interface Staker {
  publicKey: string;
  memo: string;
  staked: string;
  pendingWithdrawal: string;
  sequenceNum: bigint;
}

/** DataRequest is a data request object. */
export interface DataRequest {
  /** Hex-encoded ID of the data request */
  iD: string;
  /** Semantic versioning */
  version: string;
  /** Hex-encoded ID (hash) of the execution program */
  execProgramID: string;
  /** Inputs for the execution program */
  execInputs: Uint8Array;
  /** Gas limit for execution program execution */
  execGasLimit: bigint;
  /** Hex-encoded ID (hash) of the tally program */
  tallyProgramID: string;
  /** Inputs for the tally program */
  tallyInputs: Uint8Array;
  /** Gas limit for tally program execution */
  tallyGasLimit: bigint;
  /** Number of required data request executors (used as uint16) */
  replicationFactor: number;
  /** Specifications for the filter process */
  consensusFilter: Uint8Array;
  /** Amount of SEDA tokens per unit gas (used as uint128) */
  gasPrice: string;
  /** Public information attached to the data request */
  memo: Uint8Array;
  /** Set by the relayer */
  paybackAddress: Uint8Array;
  /** Set by SEDA Protocol (e.g. OEV-enabled data requests) */
  sEDAPayload: Uint8Array;
  /** Height at which the data request was posted */
  postedHeight: bigint;
  /**
   * Actual gas price derived by dividing attached funds by the total gas limit
   * (may be higher than the minimum gas price)
   */
  postedGasPrice: string;
  /** Poster of the data request (Bech32 string) */
  poster: string;
  /** Funds escrowed for the data request */
  escrow: string;
  /**
   * Timeout height of the data request (-1 if not set, i.e., under tallying
   * status)
   */
  timeoutHeight: bigint;
  /** Status of the data request */
  status: DataRequestStatus;
}

/** RevealBody is the content of a reveal. */
export interface RevealBody {
  /** Hex-encoded ID of the data request */
  drID: string;
  /** Block height of the data request posting */
  drBlockHeight: bigint;
  /** Exit code of the data request execution (used as uint8) */
  exitCode: number;
  /** Gas used by the data request execution */
  gasUsed: bigint;
  /** Contents of the reveal */
  reveal: Uint8Array;
  /** List of data proxy public keys used */
  proxyPubKeys: string[];
}

/** Params defines the parameters for the core module. */
export interface Params {
  dataRequestConfig: DataRequestConfig | undefined;
  stakingConfig: StakingConfig | undefined;
  tallyConfig: TallyConfig | undefined;
}

/** DataRequestConfig defines data request-related parameters. */
export interface DataRequestConfig {
  /**
   * Number of blocks after which a data request is timed out while waiting
   * for commits.
   */
  commitTimeoutInBlocks: number;
  /**
   * Number of blocks after which a data request is timed out while waiting
   * for reveals.
   */
  revealTimeoutInBlocks: number;
  /**
   * Delay in blocks before the backup executors are allowed to start executing
   * the data request.
   */
  backupDelayInBlocks: number;
  /** Maximum size of all the reveals in a data request. */
  drRevealSizeLimitInBytes: number;
  /** Maximum size of the input for the execution program. */
  execInputLimitInBytes: number;
  /** Maximum size of the input for the tally program. */
  tallyInputLimitInBytes: number;
  /** Maximum size of the consensus filter. */
  consensusFilterLimitInBytes: number;
  /** Maximum size of the memo. */
  memoLimitInBytes: number;
  /** Maximum size of the payback address. */
  paybackAddressLimitInBytes: number;
  /** Maximum size of the SEDA payload. */
  sEDAPayloadLimitInBytes: number;
}

/** StakingConfig defines staking-related parameters. */
export interface StakingConfig {
  /**
   * MiminumStake is the minimum amount of SEDA tokens required to register as
   * an Overlay.
   */
  minimumStake: string;
  /** AllowlistEnabled is a flag that indicates whether the allowlist is enabled. */
  allowlistEnabled: boolean;
}

/** TallyConfig definfes tally-related parameters. */
export interface TallyConfig {
  /** MaxTallyGasLimit is the maximum gas limit for a tally request. */
  maxTallyGasLimit: bigint;
  /** FilterGasCostNone is the gas cost for a filter type none. */
  filterGasCostNone: bigint;
  /**
   * FilterGasCostMultiplierMode is the gas cost multiplier for a filter type
   * mode.
   */
  filterGasCostMultiplierMode: bigint;
  /**
   * FilterGasCostMAD is the gas cost multiplier for a filter type
   * Median Absolute Deviation.
   */
  filterGasCostMultiplierMAD: bigint;
  /** BaseGasCost is the base gas cost for a data request. */
  baseGasCost: bigint;
  /**
   * GasCostFallback is the gas cost incurred for data request execution when
   * even basic consensus has not been reached.
   */
  executionGasCostFallback: bigint;
  /**
   * BurnRatio is the ratio of the gas cost to be burned in case of reduced
   * payout scenarios.
   */
  burnRatio: string;
  /** MaxResultSize is the maximum size of the result of a data request in bytes. */
  maxResultSize: number;
  /** MaxTalliesPerBlock specifies the maximum number of tallies per block. */
  maxTalliesPerBlock: number;
}

function createBaseStaker(): Staker {
  return { publicKey: "", memo: "", staked: "", pendingWithdrawal: "", sequenceNum: 0n };
}

export const Staker = {
  encode(message: Staker, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.publicKey !== "") {
      writer.uint32(10).string(message.publicKey);
    }
    if (message.memo !== "") {
      writer.uint32(18).string(message.memo);
    }
    if (message.staked !== "") {
      writer.uint32(26).string(message.staked);
    }
    if (message.pendingWithdrawal !== "") {
      writer.uint32(34).string(message.pendingWithdrawal);
    }
    if (message.sequenceNum !== 0n) {
      if (BigInt.asUintN(64, message.sequenceNum) !== message.sequenceNum) {
        throw new globalThis.Error("value provided for field message.sequenceNum of type uint64 too large");
      }
      writer.uint32(40).uint64(message.sequenceNum.toString());
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Staker {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStaker();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.publicKey = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.memo = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.staked = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.pendingWithdrawal = reader.string();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.sequenceNum = longToBigint(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Staker {
    return {
      publicKey: isSet(object.publicKey) ? globalThis.String(object.publicKey) : "",
      memo: isSet(object.memo) ? globalThis.String(object.memo) : "",
      staked: isSet(object.staked) ? globalThis.String(object.staked) : "",
      pendingWithdrawal: isSet(object.pendingWithdrawal) ? globalThis.String(object.pendingWithdrawal) : "",
      sequenceNum: isSet(object.sequenceNum) ? BigInt(object.sequenceNum) : 0n,
    };
  },

  toJSON(message: Staker): unknown {
    const obj: any = {};
    if (message.publicKey !== "") {
      obj.publicKey = message.publicKey;
    }
    if (message.memo !== "") {
      obj.memo = message.memo;
    }
    if (message.staked !== "") {
      obj.staked = message.staked;
    }
    if (message.pendingWithdrawal !== "") {
      obj.pendingWithdrawal = message.pendingWithdrawal;
    }
    if (message.sequenceNum !== 0n) {
      obj.sequenceNum = message.sequenceNum.toString();
    }
    return obj;
  },

  create(base?: DeepPartial<Staker>): Staker {
    return Staker.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Staker>): Staker {
    const message = createBaseStaker();
    message.publicKey = object.publicKey ?? "";
    message.memo = object.memo ?? "";
    message.staked = object.staked ?? "";
    message.pendingWithdrawal = object.pendingWithdrawal ?? "";
    message.sequenceNum = object.sequenceNum ?? 0n;
    return message;
  },
};

function createBaseDataRequest(): DataRequest {
  return {
    iD: "",
    version: "",
    execProgramID: "",
    execInputs: new Uint8Array(0),
    execGasLimit: 0n,
    tallyProgramID: "",
    tallyInputs: new Uint8Array(0),
    tallyGasLimit: 0n,
    replicationFactor: 0,
    consensusFilter: new Uint8Array(0),
    gasPrice: "",
    memo: new Uint8Array(0),
    paybackAddress: new Uint8Array(0),
    sEDAPayload: new Uint8Array(0),
    postedHeight: 0n,
    postedGasPrice: "",
    poster: "",
    escrow: "",
    timeoutHeight: 0n,
    status: 0,
  };
}

export const DataRequest = {
  encode(message: DataRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.iD !== "") {
      writer.uint32(10).string(message.iD);
    }
    if (message.version !== "") {
      writer.uint32(18).string(message.version);
    }
    if (message.execProgramID !== "") {
      writer.uint32(26).string(message.execProgramID);
    }
    if (message.execInputs.length !== 0) {
      writer.uint32(34).bytes(message.execInputs);
    }
    if (message.execGasLimit !== 0n) {
      if (BigInt.asUintN(64, message.execGasLimit) !== message.execGasLimit) {
        throw new globalThis.Error("value provided for field message.execGasLimit of type uint64 too large");
      }
      writer.uint32(40).uint64(message.execGasLimit.toString());
    }
    if (message.tallyProgramID !== "") {
      writer.uint32(50).string(message.tallyProgramID);
    }
    if (message.tallyInputs.length !== 0) {
      writer.uint32(58).bytes(message.tallyInputs);
    }
    if (message.tallyGasLimit !== 0n) {
      if (BigInt.asUintN(64, message.tallyGasLimit) !== message.tallyGasLimit) {
        throw new globalThis.Error("value provided for field message.tallyGasLimit of type uint64 too large");
      }
      writer.uint32(64).uint64(message.tallyGasLimit.toString());
    }
    if (message.replicationFactor !== 0) {
      writer.uint32(72).uint32(message.replicationFactor);
    }
    if (message.consensusFilter.length !== 0) {
      writer.uint32(82).bytes(message.consensusFilter);
    }
    if (message.gasPrice !== "") {
      writer.uint32(90).string(message.gasPrice);
    }
    if (message.memo.length !== 0) {
      writer.uint32(98).bytes(message.memo);
    }
    if (message.paybackAddress.length !== 0) {
      writer.uint32(106).bytes(message.paybackAddress);
    }
    if (message.sEDAPayload.length !== 0) {
      writer.uint32(114).bytes(message.sEDAPayload);
    }
    if (message.postedHeight !== 0n) {
      if (BigInt.asIntN(64, message.postedHeight) !== message.postedHeight) {
        throw new globalThis.Error("value provided for field message.postedHeight of type int64 too large");
      }
      writer.uint32(120).int64(message.postedHeight.toString());
    }
    if (message.postedGasPrice !== "") {
      writer.uint32(130).string(message.postedGasPrice);
    }
    if (message.poster !== "") {
      writer.uint32(146).string(message.poster);
    }
    if (message.escrow !== "") {
      writer.uint32(154).string(message.escrow);
    }
    if (message.timeoutHeight !== 0n) {
      if (BigInt.asIntN(64, message.timeoutHeight) !== message.timeoutHeight) {
        throw new globalThis.Error("value provided for field message.timeoutHeight of type int64 too large");
      }
      writer.uint32(160).int64(message.timeoutHeight.toString());
    }
    if (message.status !== 0) {
      writer.uint32(168).int32(message.status);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DataRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDataRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.iD = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.version = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.execProgramID = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.execInputs = reader.bytes();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.execGasLimit = longToBigint(reader.uint64() as Long);
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.tallyProgramID = reader.string();
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.tallyInputs = reader.bytes();
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.tallyGasLimit = longToBigint(reader.uint64() as Long);
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.replicationFactor = reader.uint32();
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.consensusFilter = reader.bytes();
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.gasPrice = reader.string();
          continue;
        case 12:
          if (tag !== 98) {
            break;
          }

          message.memo = reader.bytes();
          continue;
        case 13:
          if (tag !== 106) {
            break;
          }

          message.paybackAddress = reader.bytes();
          continue;
        case 14:
          if (tag !== 114) {
            break;
          }

          message.sEDAPayload = reader.bytes();
          continue;
        case 15:
          if (tag !== 120) {
            break;
          }

          message.postedHeight = longToBigint(reader.int64() as Long);
          continue;
        case 16:
          if (tag !== 130) {
            break;
          }

          message.postedGasPrice = reader.string();
          continue;
        case 18:
          if (tag !== 146) {
            break;
          }

          message.poster = reader.string();
          continue;
        case 19:
          if (tag !== 154) {
            break;
          }

          message.escrow = reader.string();
          continue;
        case 20:
          if (tag !== 160) {
            break;
          }

          message.timeoutHeight = longToBigint(reader.int64() as Long);
          continue;
        case 21:
          if (tag !== 168) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DataRequest {
    return {
      iD: isSet(object.iD) ? globalThis.String(object.iD) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      execProgramID: isSet(object.execProgramID) ? globalThis.String(object.execProgramID) : "",
      execInputs: isSet(object.execInputs) ? bytesFromBase64(object.execInputs) : new Uint8Array(0),
      execGasLimit: isSet(object.execGasLimit) ? BigInt(object.execGasLimit) : 0n,
      tallyProgramID: isSet(object.tallyProgramID) ? globalThis.String(object.tallyProgramID) : "",
      tallyInputs: isSet(object.tallyInputs) ? bytesFromBase64(object.tallyInputs) : new Uint8Array(0),
      tallyGasLimit: isSet(object.tallyGasLimit) ? BigInt(object.tallyGasLimit) : 0n,
      replicationFactor: isSet(object.replicationFactor) ? globalThis.Number(object.replicationFactor) : 0,
      consensusFilter: isSet(object.consensusFilter) ? bytesFromBase64(object.consensusFilter) : new Uint8Array(0),
      gasPrice: isSet(object.gasPrice) ? globalThis.String(object.gasPrice) : "",
      memo: isSet(object.memo) ? bytesFromBase64(object.memo) : new Uint8Array(0),
      paybackAddress: isSet(object.paybackAddress) ? bytesFromBase64(object.paybackAddress) : new Uint8Array(0),
      sEDAPayload: isSet(object.sEDAPayload) ? bytesFromBase64(object.sEDAPayload) : new Uint8Array(0),
      postedHeight: isSet(object.postedHeight) ? BigInt(object.postedHeight) : 0n,
      postedGasPrice: isSet(object.postedGasPrice) ? globalThis.String(object.postedGasPrice) : "",
      poster: isSet(object.poster) ? globalThis.String(object.poster) : "",
      escrow: isSet(object.escrow) ? globalThis.String(object.escrow) : "",
      timeoutHeight: isSet(object.timeoutHeight) ? BigInt(object.timeoutHeight) : 0n,
      status: isSet(object.status) ? dataRequestStatusFromJSON(object.status) : 0,
    };
  },

  toJSON(message: DataRequest): unknown {
    const obj: any = {};
    if (message.iD !== "") {
      obj.iD = message.iD;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.execProgramID !== "") {
      obj.execProgramID = message.execProgramID;
    }
    if (message.execInputs.length !== 0) {
      obj.execInputs = base64FromBytes(message.execInputs);
    }
    if (message.execGasLimit !== 0n) {
      obj.execGasLimit = message.execGasLimit.toString();
    }
    if (message.tallyProgramID !== "") {
      obj.tallyProgramID = message.tallyProgramID;
    }
    if (message.tallyInputs.length !== 0) {
      obj.tallyInputs = base64FromBytes(message.tallyInputs);
    }
    if (message.tallyGasLimit !== 0n) {
      obj.tallyGasLimit = message.tallyGasLimit.toString();
    }
    if (message.replicationFactor !== 0) {
      obj.replicationFactor = Math.round(message.replicationFactor);
    }
    if (message.consensusFilter.length !== 0) {
      obj.consensusFilter = base64FromBytes(message.consensusFilter);
    }
    if (message.gasPrice !== "") {
      obj.gasPrice = message.gasPrice;
    }
    if (message.memo.length !== 0) {
      obj.memo = base64FromBytes(message.memo);
    }
    if (message.paybackAddress.length !== 0) {
      obj.paybackAddress = base64FromBytes(message.paybackAddress);
    }
    if (message.sEDAPayload.length !== 0) {
      obj.sEDAPayload = base64FromBytes(message.sEDAPayload);
    }
    if (message.postedHeight !== 0n) {
      obj.postedHeight = message.postedHeight.toString();
    }
    if (message.postedGasPrice !== "") {
      obj.postedGasPrice = message.postedGasPrice;
    }
    if (message.poster !== "") {
      obj.poster = message.poster;
    }
    if (message.escrow !== "") {
      obj.escrow = message.escrow;
    }
    if (message.timeoutHeight !== 0n) {
      obj.timeoutHeight = message.timeoutHeight.toString();
    }
    if (message.status !== 0) {
      obj.status = dataRequestStatusToJSON(message.status);
    }
    return obj;
  },

  create(base?: DeepPartial<DataRequest>): DataRequest {
    return DataRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DataRequest>): DataRequest {
    const message = createBaseDataRequest();
    message.iD = object.iD ?? "";
    message.version = object.version ?? "";
    message.execProgramID = object.execProgramID ?? "";
    message.execInputs = object.execInputs ?? new Uint8Array(0);
    message.execGasLimit = object.execGasLimit ?? 0n;
    message.tallyProgramID = object.tallyProgramID ?? "";
    message.tallyInputs = object.tallyInputs ?? new Uint8Array(0);
    message.tallyGasLimit = object.tallyGasLimit ?? 0n;
    message.replicationFactor = object.replicationFactor ?? 0;
    message.consensusFilter = object.consensusFilter ?? new Uint8Array(0);
    message.gasPrice = object.gasPrice ?? "";
    message.memo = object.memo ?? new Uint8Array(0);
    message.paybackAddress = object.paybackAddress ?? new Uint8Array(0);
    message.sEDAPayload = object.sEDAPayload ?? new Uint8Array(0);
    message.postedHeight = object.postedHeight ?? 0n;
    message.postedGasPrice = object.postedGasPrice ?? "";
    message.poster = object.poster ?? "";
    message.escrow = object.escrow ?? "";
    message.timeoutHeight = object.timeoutHeight ?? 0n;
    message.status = object.status ?? 0;
    return message;
  },
};

function createBaseRevealBody(): RevealBody {
  return { drID: "", drBlockHeight: 0n, exitCode: 0, gasUsed: 0n, reveal: new Uint8Array(0), proxyPubKeys: [] };
}

export const RevealBody = {
  encode(message: RevealBody, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.drID !== "") {
      writer.uint32(10).string(message.drID);
    }
    if (message.drBlockHeight !== 0n) {
      if (BigInt.asUintN(64, message.drBlockHeight) !== message.drBlockHeight) {
        throw new globalThis.Error("value provided for field message.drBlockHeight of type uint64 too large");
      }
      writer.uint32(16).uint64(message.drBlockHeight.toString());
    }
    if (message.exitCode !== 0) {
      writer.uint32(24).uint32(message.exitCode);
    }
    if (message.gasUsed !== 0n) {
      if (BigInt.asUintN(64, message.gasUsed) !== message.gasUsed) {
        throw new globalThis.Error("value provided for field message.gasUsed of type uint64 too large");
      }
      writer.uint32(32).uint64(message.gasUsed.toString());
    }
    if (message.reveal.length !== 0) {
      writer.uint32(42).bytes(message.reveal);
    }
    for (const v of message.proxyPubKeys) {
      writer.uint32(50).string(v!);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RevealBody {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRevealBody();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.drID = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.drBlockHeight = longToBigint(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.exitCode = reader.uint32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.gasUsed = longToBigint(reader.uint64() as Long);
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.reveal = reader.bytes();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.proxyPubKeys.push(reader.string());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RevealBody {
    return {
      drID: isSet(object.drID) ? globalThis.String(object.drID) : "",
      drBlockHeight: isSet(object.drBlockHeight) ? BigInt(object.drBlockHeight) : 0n,
      exitCode: isSet(object.exitCode) ? globalThis.Number(object.exitCode) : 0,
      gasUsed: isSet(object.gasUsed) ? BigInt(object.gasUsed) : 0n,
      reveal: isSet(object.reveal) ? bytesFromBase64(object.reveal) : new Uint8Array(0),
      proxyPubKeys: globalThis.Array.isArray(object?.proxyPubKeys)
        ? object.proxyPubKeys.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: RevealBody): unknown {
    const obj: any = {};
    if (message.drID !== "") {
      obj.drID = message.drID;
    }
    if (message.drBlockHeight !== 0n) {
      obj.drBlockHeight = message.drBlockHeight.toString();
    }
    if (message.exitCode !== 0) {
      obj.exitCode = Math.round(message.exitCode);
    }
    if (message.gasUsed !== 0n) {
      obj.gasUsed = message.gasUsed.toString();
    }
    if (message.reveal.length !== 0) {
      obj.reveal = base64FromBytes(message.reveal);
    }
    if (message.proxyPubKeys?.length) {
      obj.proxyPubKeys = message.proxyPubKeys;
    }
    return obj;
  },

  create(base?: DeepPartial<RevealBody>): RevealBody {
    return RevealBody.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RevealBody>): RevealBody {
    const message = createBaseRevealBody();
    message.drID = object.drID ?? "";
    message.drBlockHeight = object.drBlockHeight ?? 0n;
    message.exitCode = object.exitCode ?? 0;
    message.gasUsed = object.gasUsed ?? 0n;
    message.reveal = object.reveal ?? new Uint8Array(0);
    message.proxyPubKeys = object.proxyPubKeys?.map((e) => e) || [];
    return message;
  },
};

function createBaseParams(): Params {
  return { dataRequestConfig: undefined, stakingConfig: undefined, tallyConfig: undefined };
}

export const Params = {
  encode(message: Params, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.dataRequestConfig !== undefined) {
      DataRequestConfig.encode(message.dataRequestConfig, writer.uint32(10).fork()).ldelim();
    }
    if (message.stakingConfig !== undefined) {
      StakingConfig.encode(message.stakingConfig, writer.uint32(18).fork()).ldelim();
    }
    if (message.tallyConfig !== undefined) {
      TallyConfig.encode(message.tallyConfig, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Params {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.dataRequestConfig = DataRequestConfig.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.stakingConfig = StakingConfig.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.tallyConfig = TallyConfig.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Params {
    return {
      dataRequestConfig: isSet(object.dataRequestConfig)
        ? DataRequestConfig.fromJSON(object.dataRequestConfig)
        : undefined,
      stakingConfig: isSet(object.stakingConfig) ? StakingConfig.fromJSON(object.stakingConfig) : undefined,
      tallyConfig: isSet(object.tallyConfig) ? TallyConfig.fromJSON(object.tallyConfig) : undefined,
    };
  },

  toJSON(message: Params): unknown {
    const obj: any = {};
    if (message.dataRequestConfig !== undefined) {
      obj.dataRequestConfig = DataRequestConfig.toJSON(message.dataRequestConfig);
    }
    if (message.stakingConfig !== undefined) {
      obj.stakingConfig = StakingConfig.toJSON(message.stakingConfig);
    }
    if (message.tallyConfig !== undefined) {
      obj.tallyConfig = TallyConfig.toJSON(message.tallyConfig);
    }
    return obj;
  },

  create(base?: DeepPartial<Params>): Params {
    return Params.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Params>): Params {
    const message = createBaseParams();
    message.dataRequestConfig = (object.dataRequestConfig !== undefined && object.dataRequestConfig !== null)
      ? DataRequestConfig.fromPartial(object.dataRequestConfig)
      : undefined;
    message.stakingConfig = (object.stakingConfig !== undefined && object.stakingConfig !== null)
      ? StakingConfig.fromPartial(object.stakingConfig)
      : undefined;
    message.tallyConfig = (object.tallyConfig !== undefined && object.tallyConfig !== null)
      ? TallyConfig.fromPartial(object.tallyConfig)
      : undefined;
    return message;
  },
};

function createBaseDataRequestConfig(): DataRequestConfig {
  return {
    commitTimeoutInBlocks: 0,
    revealTimeoutInBlocks: 0,
    backupDelayInBlocks: 0,
    drRevealSizeLimitInBytes: 0,
    execInputLimitInBytes: 0,
    tallyInputLimitInBytes: 0,
    consensusFilterLimitInBytes: 0,
    memoLimitInBytes: 0,
    paybackAddressLimitInBytes: 0,
    sEDAPayloadLimitInBytes: 0,
  };
}

export const DataRequestConfig = {
  encode(message: DataRequestConfig, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.commitTimeoutInBlocks !== 0) {
      writer.uint32(8).uint32(message.commitTimeoutInBlocks);
    }
    if (message.revealTimeoutInBlocks !== 0) {
      writer.uint32(16).uint32(message.revealTimeoutInBlocks);
    }
    if (message.backupDelayInBlocks !== 0) {
      writer.uint32(24).uint32(message.backupDelayInBlocks);
    }
    if (message.drRevealSizeLimitInBytes !== 0) {
      writer.uint32(32).uint32(message.drRevealSizeLimitInBytes);
    }
    if (message.execInputLimitInBytes !== 0) {
      writer.uint32(40).uint32(message.execInputLimitInBytes);
    }
    if (message.tallyInputLimitInBytes !== 0) {
      writer.uint32(48).uint32(message.tallyInputLimitInBytes);
    }
    if (message.consensusFilterLimitInBytes !== 0) {
      writer.uint32(56).uint32(message.consensusFilterLimitInBytes);
    }
    if (message.memoLimitInBytes !== 0) {
      writer.uint32(64).uint32(message.memoLimitInBytes);
    }
    if (message.paybackAddressLimitInBytes !== 0) {
      writer.uint32(72).uint32(message.paybackAddressLimitInBytes);
    }
    if (message.sEDAPayloadLimitInBytes !== 0) {
      writer.uint32(80).uint32(message.sEDAPayloadLimitInBytes);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DataRequestConfig {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDataRequestConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.commitTimeoutInBlocks = reader.uint32();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.revealTimeoutInBlocks = reader.uint32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.backupDelayInBlocks = reader.uint32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.drRevealSizeLimitInBytes = reader.uint32();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.execInputLimitInBytes = reader.uint32();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.tallyInputLimitInBytes = reader.uint32();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.consensusFilterLimitInBytes = reader.uint32();
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.memoLimitInBytes = reader.uint32();
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.paybackAddressLimitInBytes = reader.uint32();
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.sEDAPayloadLimitInBytes = reader.uint32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DataRequestConfig {
    return {
      commitTimeoutInBlocks: isSet(object.commitTimeoutInBlocks) ? globalThis.Number(object.commitTimeoutInBlocks) : 0,
      revealTimeoutInBlocks: isSet(object.revealTimeoutInBlocks) ? globalThis.Number(object.revealTimeoutInBlocks) : 0,
      backupDelayInBlocks: isSet(object.backupDelayInBlocks) ? globalThis.Number(object.backupDelayInBlocks) : 0,
      drRevealSizeLimitInBytes: isSet(object.drRevealSizeLimitInBytes)
        ? globalThis.Number(object.drRevealSizeLimitInBytes)
        : 0,
      execInputLimitInBytes: isSet(object.execInputLimitInBytes) ? globalThis.Number(object.execInputLimitInBytes) : 0,
      tallyInputLimitInBytes: isSet(object.tallyInputLimitInBytes)
        ? globalThis.Number(object.tallyInputLimitInBytes)
        : 0,
      consensusFilterLimitInBytes: isSet(object.consensusFilterLimitInBytes)
        ? globalThis.Number(object.consensusFilterLimitInBytes)
        : 0,
      memoLimitInBytes: isSet(object.memoLimitInBytes) ? globalThis.Number(object.memoLimitInBytes) : 0,
      paybackAddressLimitInBytes: isSet(object.paybackAddressLimitInBytes)
        ? globalThis.Number(object.paybackAddressLimitInBytes)
        : 0,
      sEDAPayloadLimitInBytes: isSet(object.sEDAPayloadLimitInBytes)
        ? globalThis.Number(object.sEDAPayloadLimitInBytes)
        : 0,
    };
  },

  toJSON(message: DataRequestConfig): unknown {
    const obj: any = {};
    if (message.commitTimeoutInBlocks !== 0) {
      obj.commitTimeoutInBlocks = Math.round(message.commitTimeoutInBlocks);
    }
    if (message.revealTimeoutInBlocks !== 0) {
      obj.revealTimeoutInBlocks = Math.round(message.revealTimeoutInBlocks);
    }
    if (message.backupDelayInBlocks !== 0) {
      obj.backupDelayInBlocks = Math.round(message.backupDelayInBlocks);
    }
    if (message.drRevealSizeLimitInBytes !== 0) {
      obj.drRevealSizeLimitInBytes = Math.round(message.drRevealSizeLimitInBytes);
    }
    if (message.execInputLimitInBytes !== 0) {
      obj.execInputLimitInBytes = Math.round(message.execInputLimitInBytes);
    }
    if (message.tallyInputLimitInBytes !== 0) {
      obj.tallyInputLimitInBytes = Math.round(message.tallyInputLimitInBytes);
    }
    if (message.consensusFilterLimitInBytes !== 0) {
      obj.consensusFilterLimitInBytes = Math.round(message.consensusFilterLimitInBytes);
    }
    if (message.memoLimitInBytes !== 0) {
      obj.memoLimitInBytes = Math.round(message.memoLimitInBytes);
    }
    if (message.paybackAddressLimitInBytes !== 0) {
      obj.paybackAddressLimitInBytes = Math.round(message.paybackAddressLimitInBytes);
    }
    if (message.sEDAPayloadLimitInBytes !== 0) {
      obj.sEDAPayloadLimitInBytes = Math.round(message.sEDAPayloadLimitInBytes);
    }
    return obj;
  },

  create(base?: DeepPartial<DataRequestConfig>): DataRequestConfig {
    return DataRequestConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DataRequestConfig>): DataRequestConfig {
    const message = createBaseDataRequestConfig();
    message.commitTimeoutInBlocks = object.commitTimeoutInBlocks ?? 0;
    message.revealTimeoutInBlocks = object.revealTimeoutInBlocks ?? 0;
    message.backupDelayInBlocks = object.backupDelayInBlocks ?? 0;
    message.drRevealSizeLimitInBytes = object.drRevealSizeLimitInBytes ?? 0;
    message.execInputLimitInBytes = object.execInputLimitInBytes ?? 0;
    message.tallyInputLimitInBytes = object.tallyInputLimitInBytes ?? 0;
    message.consensusFilterLimitInBytes = object.consensusFilterLimitInBytes ?? 0;
    message.memoLimitInBytes = object.memoLimitInBytes ?? 0;
    message.paybackAddressLimitInBytes = object.paybackAddressLimitInBytes ?? 0;
    message.sEDAPayloadLimitInBytes = object.sEDAPayloadLimitInBytes ?? 0;
    return message;
  },
};

function createBaseStakingConfig(): StakingConfig {
  return { minimumStake: "", allowlistEnabled: false };
}

export const StakingConfig = {
  encode(message: StakingConfig, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.minimumStake !== "") {
      writer.uint32(10).string(message.minimumStake);
    }
    if (message.allowlistEnabled !== false) {
      writer.uint32(16).bool(message.allowlistEnabled);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): StakingConfig {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStakingConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.minimumStake = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.allowlistEnabled = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StakingConfig {
    return {
      minimumStake: isSet(object.minimumStake) ? globalThis.String(object.minimumStake) : "",
      allowlistEnabled: isSet(object.allowlistEnabled) ? globalThis.Boolean(object.allowlistEnabled) : false,
    };
  },

  toJSON(message: StakingConfig): unknown {
    const obj: any = {};
    if (message.minimumStake !== "") {
      obj.minimumStake = message.minimumStake;
    }
    if (message.allowlistEnabled !== false) {
      obj.allowlistEnabled = message.allowlistEnabled;
    }
    return obj;
  },

  create(base?: DeepPartial<StakingConfig>): StakingConfig {
    return StakingConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StakingConfig>): StakingConfig {
    const message = createBaseStakingConfig();
    message.minimumStake = object.minimumStake ?? "";
    message.allowlistEnabled = object.allowlistEnabled ?? false;
    return message;
  },
};

function createBaseTallyConfig(): TallyConfig {
  return {
    maxTallyGasLimit: 0n,
    filterGasCostNone: 0n,
    filterGasCostMultiplierMode: 0n,
    filterGasCostMultiplierMAD: 0n,
    baseGasCost: 0n,
    executionGasCostFallback: 0n,
    burnRatio: "",
    maxResultSize: 0,
    maxTalliesPerBlock: 0,
  };
}

export const TallyConfig = {
  encode(message: TallyConfig, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.maxTallyGasLimit !== 0n) {
      if (BigInt.asUintN(64, message.maxTallyGasLimit) !== message.maxTallyGasLimit) {
        throw new globalThis.Error("value provided for field message.maxTallyGasLimit of type uint64 too large");
      }
      writer.uint32(8).uint64(message.maxTallyGasLimit.toString());
    }
    if (message.filterGasCostNone !== 0n) {
      if (BigInt.asUintN(64, message.filterGasCostNone) !== message.filterGasCostNone) {
        throw new globalThis.Error("value provided for field message.filterGasCostNone of type uint64 too large");
      }
      writer.uint32(16).uint64(message.filterGasCostNone.toString());
    }
    if (message.filterGasCostMultiplierMode !== 0n) {
      if (BigInt.asUintN(64, message.filterGasCostMultiplierMode) !== message.filterGasCostMultiplierMode) {
        throw new globalThis.Error(
          "value provided for field message.filterGasCostMultiplierMode of type uint64 too large",
        );
      }
      writer.uint32(24).uint64(message.filterGasCostMultiplierMode.toString());
    }
    if (message.filterGasCostMultiplierMAD !== 0n) {
      if (BigInt.asUintN(64, message.filterGasCostMultiplierMAD) !== message.filterGasCostMultiplierMAD) {
        throw new globalThis.Error(
          "value provided for field message.filterGasCostMultiplierMAD of type uint64 too large",
        );
      }
      writer.uint32(32).uint64(message.filterGasCostMultiplierMAD.toString());
    }
    if (message.baseGasCost !== 0n) {
      if (BigInt.asUintN(64, message.baseGasCost) !== message.baseGasCost) {
        throw new globalThis.Error("value provided for field message.baseGasCost of type uint64 too large");
      }
      writer.uint32(40).uint64(message.baseGasCost.toString());
    }
    if (message.executionGasCostFallback !== 0n) {
      if (BigInt.asUintN(64, message.executionGasCostFallback) !== message.executionGasCostFallback) {
        throw new globalThis.Error(
          "value provided for field message.executionGasCostFallback of type uint64 too large",
        );
      }
      writer.uint32(48).uint64(message.executionGasCostFallback.toString());
    }
    if (message.burnRatio !== "") {
      writer.uint32(58).string(message.burnRatio);
    }
    if (message.maxResultSize !== 0) {
      writer.uint32(64).uint32(message.maxResultSize);
    }
    if (message.maxTalliesPerBlock !== 0) {
      writer.uint32(72).uint32(message.maxTalliesPerBlock);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TallyConfig {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTallyConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.maxTallyGasLimit = longToBigint(reader.uint64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.filterGasCostNone = longToBigint(reader.uint64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.filterGasCostMultiplierMode = longToBigint(reader.uint64() as Long);
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.filterGasCostMultiplierMAD = longToBigint(reader.uint64() as Long);
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.baseGasCost = longToBigint(reader.uint64() as Long);
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.executionGasCostFallback = longToBigint(reader.uint64() as Long);
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.burnRatio = reader.string();
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.maxResultSize = reader.uint32();
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.maxTalliesPerBlock = reader.uint32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TallyConfig {
    return {
      maxTallyGasLimit: isSet(object.maxTallyGasLimit) ? BigInt(object.maxTallyGasLimit) : 0n,
      filterGasCostNone: isSet(object.filterGasCostNone) ? BigInt(object.filterGasCostNone) : 0n,
      filterGasCostMultiplierMode: isSet(object.filterGasCostMultiplierMode)
        ? BigInt(object.filterGasCostMultiplierMode)
        : 0n,
      filterGasCostMultiplierMAD: isSet(object.filterGasCostMultiplierMAD)
        ? BigInt(object.filterGasCostMultiplierMAD)
        : 0n,
      baseGasCost: isSet(object.baseGasCost) ? BigInt(object.baseGasCost) : 0n,
      executionGasCostFallback: isSet(object.executionGasCostFallback) ? BigInt(object.executionGasCostFallback) : 0n,
      burnRatio: isSet(object.burnRatio) ? globalThis.String(object.burnRatio) : "",
      maxResultSize: isSet(object.maxResultSize) ? globalThis.Number(object.maxResultSize) : 0,
      maxTalliesPerBlock: isSet(object.maxTalliesPerBlock) ? globalThis.Number(object.maxTalliesPerBlock) : 0,
    };
  },

  toJSON(message: TallyConfig): unknown {
    const obj: any = {};
    if (message.maxTallyGasLimit !== 0n) {
      obj.maxTallyGasLimit = message.maxTallyGasLimit.toString();
    }
    if (message.filterGasCostNone !== 0n) {
      obj.filterGasCostNone = message.filterGasCostNone.toString();
    }
    if (message.filterGasCostMultiplierMode !== 0n) {
      obj.filterGasCostMultiplierMode = message.filterGasCostMultiplierMode.toString();
    }
    if (message.filterGasCostMultiplierMAD !== 0n) {
      obj.filterGasCostMultiplierMAD = message.filterGasCostMultiplierMAD.toString();
    }
    if (message.baseGasCost !== 0n) {
      obj.baseGasCost = message.baseGasCost.toString();
    }
    if (message.executionGasCostFallback !== 0n) {
      obj.executionGasCostFallback = message.executionGasCostFallback.toString();
    }
    if (message.burnRatio !== "") {
      obj.burnRatio = message.burnRatio;
    }
    if (message.maxResultSize !== 0) {
      obj.maxResultSize = Math.round(message.maxResultSize);
    }
    if (message.maxTalliesPerBlock !== 0) {
      obj.maxTalliesPerBlock = Math.round(message.maxTalliesPerBlock);
    }
    return obj;
  },

  create(base?: DeepPartial<TallyConfig>): TallyConfig {
    return TallyConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TallyConfig>): TallyConfig {
    const message = createBaseTallyConfig();
    message.maxTallyGasLimit = object.maxTallyGasLimit ?? 0n;
    message.filterGasCostNone = object.filterGasCostNone ?? 0n;
    message.filterGasCostMultiplierMode = object.filterGasCostMultiplierMode ?? 0n;
    message.filterGasCostMultiplierMAD = object.filterGasCostMultiplierMAD ?? 0n;
    message.baseGasCost = object.baseGasCost ?? 0n;
    message.executionGasCostFallback = object.executionGasCostFallback ?? 0n;
    message.burnRatio = object.burnRatio ?? "";
    message.maxResultSize = object.maxResultSize ?? 0;
    message.maxTalliesPerBlock = object.maxTalliesPerBlock ?? 0;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToBigint(long: Long) {
  return BigInt(long.toString());
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
